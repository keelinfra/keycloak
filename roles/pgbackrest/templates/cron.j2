# Managed by keelinfra — pgBackRest schedule (runs on the repo host only)
30 2 * * 0 postgres pgbackrest --stanza={{ pgbackrest_stanza }} --type=full backup
30 2 * * 1-6 postgres pgbackrest --stanza={{ pgbackrest_stanza }} --type=diff backup
