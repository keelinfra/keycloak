---
# Sized for small nodes; override in group_vars for real hardware.
postgres_parameters:
  max_connections: 200
  shared_buffers: 512MB
  effective_cache_size: 1GB
  work_mem: 8MB
  maintenance_work_mem: 128MB
  wal_compression: "on"
  log_min_duration_statement: 1000
